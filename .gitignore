.DS_Store

# Raw source videos/stills live locally only — ship the optimized clips/webp instead.
# (patterns match assets/ root only, NOT the assets/venue|hero|logos/ subfolders.)
assets/*.mp4
assets/*.mov
assets/*.jpg
assets/*.jpeg
!assets/performance.mp4
# Social/link-preview image — JPG (not webp) for LinkedIn/iMessage compatibility; must ship.
!assets/about-photo-og.jpg
